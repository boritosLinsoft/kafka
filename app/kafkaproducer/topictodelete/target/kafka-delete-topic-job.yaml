apiVersion: batch/v1
kind: Job
metadata:
   name: kafka-delete-topic-job
   namespace: target
spec:
      template:
        spec:
          containers:
          - name: kafka-delete-topic
            image: quay.io/riadlinsoft/kafka-delete-destination-topic-job:latest
            imagePullPolicy: Always
          restartPolicy: Never
      backoffLimit: 4
